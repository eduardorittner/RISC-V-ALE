{
  "name": "risc-v-ale",
  "version": "1.0.0",
  "description": "RISC-V Assembly Learning Environment",
  "main": "index.html",
  "type": "module",
  "scripts": {
    "test": "vitest run && playwright test tests/integration",
    "test:unit": "vitest run",
    "test:ui": "playwright test tests/integration",
    "test:ui:headed": "playwright test tests/integration --headed",
    "typecheck": "tsc -p tsconfig.json && tsc -p tsconfig.worker.json"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "fast-check": "^4.9.0",
    "jsdom": "^30.0.1",
    "typescript": "5.9.3",
    "vitest": "^4.1.10",
    "ws": "^8.18.0"
  }
}
